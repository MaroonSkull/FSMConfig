#include <iostream>
#include "fsmconfig/state_machine.hpp"

/**
 * @file main.cpp
 * @brief Пример использования FSMConfig для сетевого протокола
 *
 * Этот пример демонстрирует использование библиотеки FSMConfig
 * для моделирования состояний сетевого протокола (Disconnected, Connecting, Connected, Error).
 */

int main() {
    std::cout << "Network Protocol Example" << std::endl;
    std::cout << "=======================" << std::endl;

    // Реализация будет добавлена в следующих подзадачах

    return 0;
}
